{
  "name": "food-delivery",
  "version": "1.0.0",
  "private": true,
  "devDependencies": {
    "concurrently": "^9.2.1"
  },
  "scripts": {
    "dev": "concurrently \"npm run server --prefix backend\" \"npm run dev --prefix frontend\" \"npm run dev --prefix admin\"",
    "install:backend": "npm install --prefix backend",
    "install:frontend": "npm install --prefix frontend",
    "install:admin": "npm install --prefix admin",
    "install:all": "node tools/install-all.js",
    "postinstall": "node -e \"const fs=require('fs');const path=require('path');const script=path.join(process.cwd(),'tools','install-all.js');if(fs.existsSync(script)){require(script);}else{console.log('[install-all] tools/install-all.js missing; skipping.');}\"",
    "seed:v2": "node backend/scripts/seedV2.js",
    "docker:up": "docker compose up --build",
    "docker:down": "docker compose down",
    "docker:logs": "docker compose logs -f backend"
  }
}
